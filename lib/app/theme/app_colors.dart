import 'package:flutter/material.dart';

class C {
  C._();

  // Primary — Teal
  static const teal      = Color(0xFF1BA8C8);
  static const tealDark  = Color(0xFF1480A0);
  static const tealLight = Color(0xFFD6F2F9);

  // Accent — Gold
  static const gold      = Color(0xFFF5A623);
  static const goldLight = Color(0xFFFEF4E0);

  // NEW — Orange brand accent
  static const orange      = Color(0xFFE67E22);
  static const orangeLight = Color(0xFFFEF3DC);
  static const orangeDark  = Color(0xFFD35400);

  // Legacy alias (pour ne pas casser l'existant)
  static const amber       = orange;
  static const amber2      = orangeLight;

  // Neutral — Navy
  static const navy      = Color(0xFF1A3A5C);
  static const navyLight = Color(0xFFE8EEF5);

  // Text
  static const ink       = Color(0xFF0D1B2A);
  static const ink2      = Color(0xFF3D4F5E);
  static const ink3      = Color(0xFF8A9BAC);
  static const ink4      = Color(0xFFB8C5D0);

  // Surfaces
  static const surface   = Color(0xFFFFFFFF);
  static const surface2  = Color(0xFFF4F8FB);
  static const surface3  = Color(0xFFE8EFF5);
  static const border    = Color(0xFFDDE6EE);

  // Semantic
  static const green     = Color(0xFF0A8754);
  static const green2    = Color(0xFFDDF4EC);
  static const green3    = Color(0xFF2ECC8A);
  static const red       = Color(0xFFD63031);
  static const red2      = Color(0xFFFEECEC);
  static const blue      = Color(0xFF2563EB);
  static const blue2     = Color(0xFFEFF6FF);
}